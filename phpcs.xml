<?xml version="1.0"?>
<ruleset name="SimpleLMS Bridge">
    <description>WordPress-Extra coding standards for SimpleLMS Bridge.</description>

    <!-- Scan the plugin root and includes; exclude generated / vendored code. -->
    <file>simple-lms-bridge.php</file>
    <file>includes</file>

    <arg name="extensions" value="php"/>
    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="parallel" value="8"/>

    <exclude-pattern>*/build/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/src/*</exclude-pattern>
    <exclude-pattern>*/dist/*</exclude-pattern>

    <rule ref="WordPress-Extra">
        <!-- The plugin already ships an established text domain and prefixes;
             leave stylistic-only sniffs advisory rather than failing CI. -->
        <exclude name="WordPress.Files.FileName"/>
        
        <!-- Exclude non-formatting sniffs to ensure PHPCS acts as a strict formatting-only gate -->
        <exclude name="WordPress.Security.EscapeOutput"/>
        <exclude name="WordPress.WP.GlobalVariablesOverride"/>
        <exclude name="WordPress.DB.PreparedSQL"/>
        <exclude name="Universal.Operators.DisallowShortTernary"/>
        <exclude name="WordPress.PHP.YodaConditions"/>
        <exclude name="WordPress.Security.NonceVerification"/>
        <exclude name="WordPress.WP.PostsPerPage"/>
        <exclude name="Generic.CodeAnalysis.UnusedFunctionParameter"/>
        <exclude name="WordPress.PHP.NoSilencedErrors"/>
        <exclude name="WordPress.WP.AlternativeFunctions"/>
        <exclude name="WordPress.PHP.DiscouragedPHPFunctions"/>
        <exclude name="WordPress.DateTime.CurrentTimeTimestamp"/>
        <exclude name="WordPress.WP.I18n"/>
        <exclude name="WordPress.PHP.DevelopmentFunctions"/>
        <exclude name="Universal.NamingConventions.NoReservedKeywordParameterNames"/>
    </rule>

    <config name="minimum_supported_wp_version" value="6.0"/>
    <config name="testVersion" value="7.4-"/>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="simple-lms-bridge"/>
            </property>
        </properties>
    </rule>
</ruleset>
